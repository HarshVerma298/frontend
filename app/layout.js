export const metadata = {
  title: 'Recruitment Portal',
  description: 'User PDF upload & Recruiter search',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
